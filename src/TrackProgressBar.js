import React from 'react';

function TrackProgressBar({currentFile}) {

    return (
        <>
            <h2>Track progress bar will be here</h2>
            <h2>Current file {currentFile}</h2>
        </>

    );
}

export default TrackProgressBar;